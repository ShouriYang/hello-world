
export default function (versionId) {
  const errorList = [
    {
      title: '高级查询',
      key: `/product/project/version/${versionId}/error`,
      icon: 'switcher'
    },

  ]
  return errorList
}